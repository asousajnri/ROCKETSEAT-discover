// função que eleva o número ao quadrado

// Imperativo: Faça da seguinte forma
let number = 2;
function square() {
  return number * number;
}
number = square();
console.log(number);

// Declativa: O que fazer e não como fazer
const square = (n) => n * n;
